# Minimal install: runs the complete multi-agent pipeline offline, with the
# deterministic encoder, the bundled dataset and the template answer generator.
# Install requirements.txt instead to enable Gemini, Tavily and FAISS.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
httpx>=0.27.0
python-dotenv>=1.0.1
pytest>=8.3.0
